import 'package:flutter/material.dart';

class AppStyle {
  static const borderRadius = BorderRadius.all(Radius.circular(14));
  static const cardPadding = EdgeInsets.all(16);
  static const statCardHeight = 88.0;
  static const pagePadding = EdgeInsets.symmetric(horizontal: 20, vertical: 16);
  static const shadow = BoxShadow(color: Color(0x22000000), blurRadius: 10, offset: Offset(0,4));
}
